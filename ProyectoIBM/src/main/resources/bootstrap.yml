management:
  endpoints:
    web:
      exposure:
        include: '*'

server:
  port: 8080
spring:
  application:
    name: application
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: development
